{
  "scripts": {
    "install" : "cd pxt && npm install && npm run build && cd ../pxt-common-packages && npm install && cd ../pxt-microbit && npm install",
    "start": "cd pxt-microbit && pxt serve",
    "dev": "cd pxt-microbit && pxt serve",
    "build": "cd pxt-microbit && pxt staticpkg"
  }
}
